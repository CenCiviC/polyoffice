# 구글 문서 기능 인벤토리 — "최소한 이만큼은" 기준선

목표: **구글 문서(Google Docs)가 가진 편집 기능을 최소 기준선으로 삼는다.** 그러려면
먼저 그 목록이 있어야 한다. 이 문서는 구글 문서 데스크톱 메뉴를 전수로 훑어
Narro의 현재 상태와 나란히 놓은 것이다.

상태 기호:

- ✅ — 편집기에서 되고, IR에 담기고, 최소 한 개 백엔드로 저장된다
- ◐ — 일부만 (읽기만 되거나, 편집은 되는데 저장에서 강등되거나, IR에 슬롯만 있음)
- ✗ — 없음
- ⊘ — **의도적으로 범위 밖** (클라우드·협업·AI. 이유는 마지막 절)

"IR 요구"는 이 기능을 넣으려면 [IR-SPEC.md](IR-SPEC.md)의 어휘·스타일 목록에
무엇을 추가해야 하는지다. 스펙 규칙 1에 따라 **hwpx·docx 매핑이 정의되기 전에는
어휘를 추가할 수 없으므로**, 그 매핑 후보도 함께 적었다.

---

## 1. 파일

| 기능 | Narro | IR 요구 / 메모 |
|---|:--:|---|
| 열기 (가져오기) | ✅ | hwp·doc·hwpx·docx·odt 5종. 구글 문서보다 넓다 (구글은 hwp를 못 읽는다) |
| 다운로드 — docx | ✅ | |
| 다운로드 — odt | ✅ | |
| 다운로드 — hwpx | ✅ | 구글 문서에 없는 것 |
| 다운로드 — pdf | ◐ | 브라우저 인쇄 대화상자 경유(`printDoc`). 직접 PDF 생성은 없음 |
| 다운로드 — html | ✅ | standalone, 폰트 서브셋 임베딩 |
| 다운로드 — rtf · txt · epub · markdown | ✗ | txt·markdown은 IR 순회로 값싸게 붙는다. rtf·epub는 백엔드 추가 |
| 페이지 설정 — 용지 크기 | ◐ | 읽기는 됨(`doc-section` width/height). **편집 UI 없음** |
| 페이지 설정 — 방향(가로/세로) | ◐ | 위와 같음. width/height 스왑이면 끝 |
| 페이지 설정 — 여백 | ◐ | 위와 같음(`padding`). 눈금자 드래그는 없음 |
| 페이지 설정 — 페이지 색 | ✗ | IR 스타일 어휘에 `background`(doc-section) 추가. hwpx `pagePr`, docx `w:background` |
| 페이지리스(pageless) 모드 | ✗ | 뷰어 전용 토글. 파일 포맷에는 안 남음 |
| 인쇄 | ✅ | |
| 사본 만들기 · 이동 · 휴지통 · 공유 | ⊘ | 저장소 개념이 없다 |
| 버전 기록 | ⊘ | 서버 필요. 로컬 IndexedDB 스냅샷으로 축소 구현은 가능 |
| 문서 언어 설정 | ✗ | 맞춤법·금칙처리에 영향. `ir-model.ts`의 `LINE_BREAK`가 지금 전역 상수 |

## 2. 수정 · 보기

| 기능 | Narro | IR 요구 / 메모 |
|---|:--:|---|
| 실행 취소 / 다시 실행 | ✅ | contentEditable 네이티브 |
| 잘라내기 · 복사 · 붙여넣기 | ✅ | |
| 서식 없이 붙여넣기 | ◐ | 붙여넣기 시 IR 밖 태그를 거르는 새니타이저는 있음(`insertHTML` 경로). 전용 단축키는 없음 |
| 찾기 및 바꾸기 | ✅ | ⌘F, 전체 바꾸기 포함 |
| — 대소문자 구분 · 정규식 · 서식 조건 | ✗ | 구글은 정규식까지 지원 |
| 확대/축소 | ✅ | 10% 단위, ⌘0 리셋 |
| 한 페이지 / 두 페이지 나란히 | ✅ | 구글 문서에 없는 것 |
| 개요(목차) 사이드 패널 | ✗ | h1–h6은 이미 IR에 있으니 **순수 뷰어 기능**. 값싸고 효과 큼 |
| 눈금자 | ✗ | 들여쓰기·탭 정지 편집의 전제 |
| 비인쇄 문자 표시(¶) | ✗ | 뷰어 CSS만으로 가능 |
| 편집 / 제안 / 보기 모드 | ◐ | 편집·보기만. 제안 모드는 ⊘ |
| 전체화면 · 메뉴 숨기기 | ✗ | |

## 3. 삽입

여기가 격차의 대부분이다.

| 기능 | Narro | IR 요구 / 메모 |
|---|:--:|---|
| 이미지 | ✅ | data URI, 인라인 앵커 고정 |
| — 자르기 · 재색상 · 밝기/대비 · 테두리 | ✗ | 잘라낸 결과를 캔버스로 구워 넣으면 IR 변경 없이 가능(원본 손실) |
| — 텍스트 줄바꿈(둘러싸기/뒤로/앞으로) · 위치 고정 | ✗ | IR에 `data-wrap`·`data-anchor` 필요. hwpx `textWrap`(이미 `TOP_AND_BOTTOM` 고정 출력 중), docx `wp:anchor` |
| — 대체 텍스트(alt) | ◐ | IR `img[alt]` 있음. 편집 UI 없음 |
| 표 삽입 | ✅ | 행/열 추가·삭제, 병합, 병합 해제 |
| — 셀 배경색 | ◐ | IR·백엔드는 됨. **편집 UI 없음** |
| — 셀 테두리(선 두께·색·종류) | ✗ | IR 스타일 어휘에 `border` 추가. 세 포맷 모두 borderFill/`w:tcBorders`/`fo:border` 보유 |
| — 셀 세로 정렬(위/중간/아래) | ✗ | `vertical-align`. hwpx `hp:cellAddr`이 아니라 `subList vertAlign` |
| — 열 너비 · 행 높이 드래그 | ◐ | IR에 pt로 있음. 드래그 UI 없음 |
| — 행 균등 배분 · 정렬(sort) · 헤더 행 고정 | ✗ | 고정은 hwpx `repeatHeader`, docx `w:tblHeader` |
| — 셀 분할(split) | ✗ | 병합의 역이지만 격자 재계산이 필요 |
| — 표↔텍스트 변환 | ✗ | |
| 가로줄 | ✗ | `<hr>` 추가. hwpx는 문단 테두리 아래선, docx `w:pBdr bottom` |
| 각주 | ◐ | **읽기 ✅, 쓰기 ✅, 편집기 삽입 UI 없음**. IR `doc-footnote` 완비 |
| 미주 | ◐ | 읽을 땐 각주로 흡수. 별도 어휘 없음 |
| 머리글 / 바닥글 | ✗ | IR `doc-header`/`doc-footer` **슬롯만 정의됨**. 매핑 신뢰도 ◐ — golden file 먼저 |
| 페이지 번호 / 전체 페이지 수 | ✗ | 머리글·바닥글의 종속 기능. 필드 개념이 IR에 없다(진실원=필드 코드, 렌더=숫자) |
| 페이지 나누기 | ✅ | `doc-pagebreak` |
| 구역 나누기(다음 페이지 / 연속) | ◐ | `doc-section`은 있으나 편집기에서 못 만듦. docx 다중 sectPr은 읽기도 미지원(README) |
| 열(단) 나누기 — 2단·3단 | ✗ | 큰 건. hwpx `hp:colPr`, docx `w:cols`, odt `style:columns` |
| 링크(하이퍼링크) | ✗ | **가장 값싼 큰 구멍.** IR에 `<a href>` 없음. 세 포맷 다 지원(hwpx `hp:fieldBegin HYPERLINK`, docx `w:hyperlink`+rels, odt `text:a`) |
| 책갈피 · 문서 내 링크 | ✗ | `data-id`가 이미 블록 주소라 앵커로 재사용 가능 |
| 목차(TOC) | ✗ | h1–h6 + `data-id`로 생성 가능. 파생물이라 IR 규칙 2상 "저장 안 함" 대상 — 필드로 넣을지 텍스트로 구울지 결정 필요 |
| 특수문자 팔레트 | ✗ | 순수 UI. IR 변경 없음 |
| 이모지 | ◐ | 입력은 됨(그냥 문자). 팔레트 없음 |
| 수식 | ✗ | IR `doc-eq[data-latex]` 슬롯 정의됨. hwpx는 한글 수식 스크립트 변환기 자작 필요 (매핑 신뢰도 ○) |
| 그림(도형·선·화살표·텍스트박스) | ✗ | 글상자는 IR `doc-textbox` 슬롯. 도형은 어휘 자체가 없음 |
| 차트 | ✗ | 실질적으로 이미지로 굽는 것 외 방법 없음 |
| 워터마크 | ✗ | hwpx·docx 모두 배경 개체 |
| 댓글 | ⊘ | |
| 스마트 칩(사람·파일·날짜·드롭다운·지도) | ⊘ | 구글 고유. 다른 포맷으로 나갈 때 표현할 대상이 없다 |
| 빌딩 블록 · 문서 탭 | ⊘ | 구글 고유 |

## 4. 서식

| 기능 | Narro | IR 요구 / 메모 |
|---|:--:|---|
| 굵게 · 기울임 · 밑줄 · 취소선 | ✅ | |
| 글꼴(서체) | ✅ | 드롭다운 + IR `font-family` + 서브셋 임베딩 |
| 글자 크기 | ✅ | |
| 글자 색 | ✅ | |
| 형광펜(음영) | ✅ | span `background` → charPr shadeColor |
| 위첨자 / 아래첨자 | ✗ | IR 스타일 어휘에 `vertical-align` 또는 `<sup>/<sub>`. 세 포맷 모두 보유 |
| 작은 대문자(small caps) | ✗ | 한글 문서엔 사실상 무의미. 후순위 |
| 대소문자 변경 | ✗ | 텍스트 변형이라 IR 변경 없음 |
| 서식 복사(paint format) | ✗ | 순수 UI |
| 서식 지우기 | ✗ | 순수 UI. `removeFormat` 한 줄에 가깝다 |
| 단락 스타일(제목1–6 / 일반) | ✅ | h1–h6. 저장 시 크기·굵기로 강등 |
| — 부제(Subtitle) · 제목(Title) | ✗ | |
| — 스타일 정의 수정 / 내 기본 스타일 | ✗ | **명명 스타일 개념이 IR에 없다.** 지금은 전부 인라인 스타일. 구조적 결정 사안 |
| 정렬 좌/중/우/양쪽 | ✅ | |
| 들여쓰기 — 늘리기 / 줄이기 | ✗ | IR에 `margin-left` 없음. 한글 공문서에 매우 흔함 |
| 들여쓰기 — 첫 줄 / 내어쓰기 | ✗ | `text-indent`. 한글 문서의 기본 관용 서식 |
| 탭 정지(tab stop) | ✗ | `\t`는 런에 흐르지만 정지 위치 개념이 없다 |
| 줄 간격 | ✅ | 편집 UI + IR `line-height` |
| 단락 앞/뒤 여백 | ✗ | 제목만 `HEADING_SPACE` 상수로 고정. IR에 `margin-top/bottom` 필요 |
| 단락 나누지 않기 · 다음과 함께 | ✗ | hwpx `keepWithNext`, docx `w:keepNext` |
| 글머리 목록 · 번호 목록 | ◐ | 편집기 ✅ / **저장 시 "• ", "1. " 텍스트로 강등**. 진짜 numbering 매핑은 v0.3 |
| — 체크리스트 | ✗ | |
| — 목록 수준(들여쓰기로 중첩) · 접두/접미 · 번호 다시 시작 | ✗ | |
| — 개요 번호(1.1.1 / 가·나·다) | ✗ | 한글 공문서의 핵심. README "아직 안 되는 것"에도 명시 |
| 단락 테두리 및 음영 | ✗ | |
| 표 속성 일괄(정렬·색·테두리) | ✗ | 위 표 항목 참조 |

## 5. 도구

| 기능 | Narro | 메모 |
|---|:--:|---|
| 맞춤법 및 문법 | ◐ | 브라우저 네이티브 맞춤법(contentEditable)에 의존. 한국어 문법 검사는 없음 |
| 단어 수 / 글자 수 | ✗ | `stats`가 이미 방출기에 있음. 표시만 하면 됨 |
| 사전 · 번역 · 인용 · 문서 비교 | ✗ | 문서 비교는 IR diff로 자연스럽다 (`data-id` 덕분) |
| 음성 입력 | ✗ | Web Speech API |
| 환경설정 — 자동 대체 · 스마트 따옴표 · 마크다운 입력 | ✗ | |
| 접근성(스크린리더) | ✗ | contentEditable + ARIA. 표 읽기 안내 등 |
| 부가기능 · Apps Script · 전자서명 · Gemini | ⊘ | |

## 6. 협업

| 기능 | Narro | |
|---|:--:|---|
| 실시간 공동 편집 · 커서 · 댓글 · 제안 모드 · @멘션 · 승인 | ⊘ | 전부 서버·계정·CRDT가 전제. Narro는 "파일이 서버로 안 올라간다"가 설계 전제라 이 축은 다른 제품이 된다 |

---

## 범위 밖(⊘)으로 두는 이유

구글 문서 기능의 상당 부분은 **에디터 기능이 아니라 클라우드 기능**이다 — 공유·버전 기록·
댓글·제안 모드·스마트 칩·Gemini. 이것들은 (1) 서버와 계정을 요구하고, (2) hwpx·docx·odt
어느 포맷으로도 나갈 표현이 없다. Narro의 전제는 "클라이언트에서만 파싱한다"이므로
이 축을 쫓으면 제품이 바뀐다. **기준선은 "구글 문서의 편집·서식 기능"으로 좁히는 게 맞다.**

단 두 개는 예외적으로 값어치가 있다: **버전 기록**(로컬 IndexedDB 스냅샷으로 축소 가능)과
**문서 비교**(IR `data-id` diff로 오히려 구글보다 잘할 수 있다).

## 우선순위 제안

기준: (a) 실제 한글·워드 문서에 나오는 빈도 × (b) 세 백엔드가 전부 표현 가능한가 ×
(c) IR 어휘 변경이 작은가.

**1군 — 격차가 크고 값싸다**

1. **하이퍼링크** — IR에 `<a href>` 하나. 세 포맷 모두 1:1 매핑. 지금 링크 있는 문서를 열면 **텍스트만 남고 링크가 소실**된다
2. **들여쓰기 / 첫 줄 들여쓰기** — `margin-left`·`text-indent`. 한글 공문서 기본 서식
3. **단락 앞/뒤 여백** — `margin-top/bottom`. 지금 제목만 상수로 박혀 있다
4. **위첨자 / 아래첨자** — 각주 번호 표시의 전제이기도 하다
5. **각주 삽입 UI** — IR·읽기·쓰기가 이미 다 되는데 편집기에서 못 넣는다. 순수 UI 작업
6. **개요 패널 · 단어 수 · 서식 지우기** — IR 변경 0, 순수 뷰어/UI

**2군 — 크지만 반드시 필요**

7. **개요 번호 · 진짜 목록 numbering** — 지금 "• " 텍스트 강등이라 왕복하면 목록이 아니게 된다. 한글 문서의 핵심
8. **머리글 / 바닥글 + 페이지 번호** — 슬롯은 있으나 매핑이 ◐. golden file부터
9. **표 셀 테두리 · 세로 정렬** — 표 충실도의 남은 절반
10. **명명 스타일(Heading 1 정의 수정)** — 인라인 스타일만 쓰는 현 구조의 한계. 손대려면 IR 구조 결정이 선행

**3군 — 크고 나중**

11. 단(열) 나누기 · 목차 · 수식 · 도형/글상자 · 이미지 줄바꿈과 위치 · 워터마크

## 출처

- [Keyboard shortcuts for Google Docs](https://support.google.com/docs/answer/179738) — 명령 전수 목록으로는 가장 완전하다
- [Tips to format & customize documents](https://support.google.com/a/users/answer/13003605)
- [Tool finder for Docs](https://support.google.com/docs/answer/13466905)
- [Google Workspace Updates 2026](https://workspaceupdates.googleblog.com/2026/) — 최근 추가분(Gemini 이미지·다이어그램 생성, 댓글 워크플로)
